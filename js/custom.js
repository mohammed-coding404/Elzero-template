/*global $, alert, console*/

$(function () {

    'use strict';
    // triger nice scroll plugin
    $('html').niceScroLL();

    //change header hieght 

    $('.header').height($(window).height());
});